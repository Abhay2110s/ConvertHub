# Base URL of the deployed ConvertHub backend (see ../backend), no trailing slash.
# Local dev (matches `vercel dev` default):
VITE_API_BASE_URL=http://localhost:3000

# Production example, once the backend is deployed on Vercel:
# VITE_API_BASE_URL=https://your-backend-project.vercel.app
